#include<bits/stdc++.h>
#include <vector>
using namespace std;
int 
main()
{
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  int t, n, l, r, x, q, h, y;
  cin >> t;
  while(t--) {
    cin >> n;
    vector<int> a;
    for(int i=0; i< n; i++) {
      cin >> h;
      a.push_back(h);
    }
    
  }
}
